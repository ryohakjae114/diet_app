const toggle_switch_ = document.getElementById("toggle_switch");
const button = document.getElementById("button");

toggle_switch.addEventListener('click', function submit(event) {
  button.click();
});
