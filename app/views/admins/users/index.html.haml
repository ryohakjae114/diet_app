%p#notice= notice
%h1 Users
%table.table
  %thead
    %tr
      %th id
      %th ニックネーム
      %th 性別
      %th{:colspan => "3"}
  %tbody
    - @users.each do |user|
      %tr
        %td= user.id
        %td= user.nickname
        %td= user.gender
        %td= link_to 'Show', admins_user_path(user)
        %td= link_to 'Edit', edit_admins_user_path(user)
        %td= link_to 'Destroy', admins_user_path(user), method: :delete, data: { confirm: 'Are you sure?' }
