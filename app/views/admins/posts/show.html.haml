%p#notice= notice

%p
  %b ユーザID:
  = @post.user_id
%p
  %b テキスト:
  = @post.text

%td= link_to '削除', admins_post_path(@post), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"

%br
\|
=link_to 'Back', admins_posts_path(user_id: @post.user_id)
