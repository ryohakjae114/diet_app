= form_with(url: search_items_url, method: :post, local: false) do |f|
  .text-center
    = f.text_field :keyword, placeholder: "アイテム名", id: "text"
    = f.submit '検索', id: "button", style: "display: none"

= javascript_pack_tag 'send_item_keyword'